# Legacy skill stubs (non-runtime)

These directories were previously at the repository root. They are **not** loaded by the application.

Canonical runtime prompts: `backend/app/agents/` (each agent's SKILL.md sits next to its
Python runner, e.g. `backend/app/agents/discovery-agent/SKILL.md`).

Content here has drifted from the backend copies (hashes differed at archive time). Keep for historical comparison; merge or discard after review.
